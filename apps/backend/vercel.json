{
    "version": 2,
    "builds": [
      {
        "src": "src/server.ts",
        "use": "@vercel/node@2.12.2",
        "config": { "runtime": "nodejs22.x" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "src/server.ts" }
    ]
  }